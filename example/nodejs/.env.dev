# Example development values for stage-based SAM deploy options.
# Keep real credentials in your CI/CD secret store.

STAGE=dev

# Dummy value for local validation of SAM_ADDOPTS/env expansion flow.
# Replace with real values via CI secret in CI/CD pipelines secrets/variables.
SAM_PARAMETER_OVERRIDES=DBUsername=dummy DBPassword=dummy
