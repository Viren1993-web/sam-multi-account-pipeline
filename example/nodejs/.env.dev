# Example development values for stage-based SAM deploy options.
# Keep real credentials in your CI/CD secret store.

STAGE=dev

# This secrets can be used for local development, but should be overridden in CI/CD pipelines with real secrets.
# SAM_PARAMETER_OVERRIDES: $SAM_PARAMETER_OVERRIDES
